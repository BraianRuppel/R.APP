<?xml version="1.0" encoding="utf-8"?>
<navigation
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/landingFragment">

    <fragment
        android:id="@+id/landingFragment"
        android:name="com.example.rapp.ui.landing.LandingFragment"
        android:label="Landing"
        tools:layout="@layout/fragment_landing">

        <action
            android:id="@+id/action_landing_to_itemList"
            app:destination="@id/itemListFragment" />

        <action
            android:id="@+id/action_landing_to_events"
            app:destination="@id/eventsFragment" />
    </fragment>

    <fragment
        android:id="@+id/itemListFragment"
        android:name="com.example.rapp.ui.itemlist.ItemListFragment"
        android:label="Lista de Items"
        tools:layout="@layout/fragment_item_list" />

    <fragment
        android:id="@+id/eventsFragment"
        android:name="com.example.rapp.ui.events.EventsFragment"
        android:label="Eventos"
        tools:layout="@layout/fragment_events" />

</navigation>